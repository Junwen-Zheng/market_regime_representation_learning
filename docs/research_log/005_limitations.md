# 005 - Limitations

The current project uses synthetic data for reproducibility. This makes the repository easy to review, but it also means the result is not evidence of live alpha. The next version should use real point-in-time equity data and stricter implementation-cost assumptions.
